// NomadCommander/Main.cpp
#include "pch.h"

// The executable's entry point. Phase 0 (NC-001) proves the build shape and nothing else: the process starts, does
// nothing and exits 0. NC-020 gives it a window. The parameters stay unnamed until something reads them; /W4 /WX
// would otherwise report them unreferenced.
int WINAPI wWinMain(HINSTANCE, HINSTANCE, PWSTR, int)
{
  return 0;
}
