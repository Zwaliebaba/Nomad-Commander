// Tests/NeuronServerTests/pch.h: the precompiled header. Include it first in every .cpp of this project (AGENTS.md §4).
// NeuronCore.h comes first so the Windows macro family is set before CppUnitTest.h brings anything from Windows.
#pragma once

#include "NeuronCore.h"

#include "CppUnitTest.h"
